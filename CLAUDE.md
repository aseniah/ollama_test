# AI Model Benchmarks

Two benchmarks for evaluating local AI model backends:

- `shell/` — iTerm2 shell command generation benchmark
- `codegen/` — Code generation benchmark

Each directory is self-contained. See the README.md and CLAUDE.md inside each for details.
