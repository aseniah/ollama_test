Read the file `input/data.json` — a JSON array of objects with fields:
name (string), age (integer), active (boolean), score (float).
Output to stdout a JSON array containing only records where active is true
and age is 30 or older, sorted by name ascending.
Do not output anything else.
