Read the file `input/data.csv` and output a JSON array to stdout.
Each row (excluding the header) becomes an object with these fields:
- Name: string
- Age: integer
- Email: string
- Score: float

Output must be valid JSON. Do not output anything else.
